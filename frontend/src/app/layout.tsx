import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';
import { Toaster } from '@/components/ui/toaster';
import { Providers } from './providers';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'CV Builder - AI-Powered Resume & Cover Letter Generator',
  description: 'Create professional CVs and cover letters customized for any job description using AI. Instant PDF downloads with ATS optimization.',
  keywords: ['cv builder', 'resume generator', 'cover letter', 'AI resume', 'job application'],
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={inter.className}>
        <Providers>
          {children}
          <Toaster />
        </Providers>
      </body>
    </html>
  );
}
